# frozen_string_literal: true

RSpec.describe Capybara::Storyboard do
  it "has a version number" do
    expect(Capybara::Storyboard::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
